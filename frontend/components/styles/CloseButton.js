import styled from "styled-components";

const CloseButton = styled.button`
  background: black;
  color: white;
  font-size: 3.3rem;
  border: -1px;
  position: absolute;
  z-index: 2;
  right: 0;
  top: 0;
`;

export default CloseButton;
